export interface ProductionData {
  partNumber: string;
  partName: string;
  quantity: number;
  date: string;
  shift: string;
  operator: string;
}

export interface ChartData {
  name: string;
  quantity: number;
}